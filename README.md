# otus-express-crud

CRUD пользователей и задач на express

`npm run dev` - запуск в dev режиме

`npm run build` - билд ts в dist

`npm run start` - запуск сервера из dist

`http://localhost:3000`
`GET /users`
`GET /users/1`
`POST /users`
`PATCH /users/1`
`DELETE /users/1`
